:80 {
    root * /usr/share/caddy
    encode gzip

    handle /api/* {
        reverse_proxy {$SERVICE_NAME_API:api}:3000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}
