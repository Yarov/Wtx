:80 {
    root * /usr/share/caddy
    encode gzip

    handle_path /api/* {
        reverse_proxy api:3000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}
